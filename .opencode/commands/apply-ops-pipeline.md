---
description: Process pending URLs from pipeline inbox
---

Process pending job URLs from data/pipeline.md using apply-ops pipeline mode.

Load the apply-ops skill:
```
skill({ name: "apply-ops" })
```
